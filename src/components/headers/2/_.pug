mixin header-2(url, icon, h1, h2, breadcrumbs, navigation, hook)
    header(class=hook ? "header-2 " + hook :  "header-2")

        .core

            h1
                a(href=url)
                    svg
                        use(xlink:href="/assets/images/_sprites/symbols.svg#" + icon)
                    =h1

            if breadcrumbs

                div

                    nav(role="navigation")

                        ul
                            for breadcrumb in breadcrumbs

                                li
                                    a(href=breadcrumb.href)= breadcrumb.name
                                    span &nbsp;/&nbsp;

                    h2= h2 || 'Heading 2'

            else

                h2= h2 || 'Heading 2'

        if navigation

            .peripheral

                nav(role="navigation")

                    ul
                        for anchor in navigation

                            li
                                a(href=anchor.href)= anchor.name
